rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Test mode: open to all reads and writes — replace with proper rules before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
